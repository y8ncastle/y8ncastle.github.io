import ContentHeader from "components/ContentHeader";

const Profile = () => {
  return (
    <main>
      <ContentHeader title={`snapshot.profile.contentHeader`} />

      <section></section>
    </main>
  );
};

export default Profile;
